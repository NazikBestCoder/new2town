<div>
<h2>Friends List</h2>
{{#each friendAll as |friend|}}
<div>
<p>
{{friend.username}}
</p>
</div>
{{/each}}

</div>

            <button id="logout" class="bg-blue-400 hover:bg-blue-600 hover:shadow-inner rounded-b-lg w-24 h-12 border-black border-2">Logout</button>

               {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
    